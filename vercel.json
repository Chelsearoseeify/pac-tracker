{
  "buildCommand": "pnpm --filter @pac/web build && node scripts/bundle-api.mjs",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api/index" }]
}
